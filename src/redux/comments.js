import { COMMENTS } from '../shared/comments';


// reducer function which manages only the comments
export const Comments = (state = COMMENTS, action) => {
    switch(action.type) {
        default:
            return state;

    }
}